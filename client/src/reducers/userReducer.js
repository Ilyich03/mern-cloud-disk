const defaultState = {
    currentUser: {},
    isAuth: false
}

export default function userReducerState(state = defaultState, action) {
    switch(action.type) {
        default: 
            return state
    }
}